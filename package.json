{
  "name": "hudi-supermarket-root",
  "private": true,
  "scripts": {
    "install-all": "cd frontend && npm install",
    "build": "cd frontend && npm install && npm run build -- --webpack && cd .. && mkdir -p .next && cp -RT frontend/.next .next && cp -RT frontend/public public",
    "start": "cd frontend && npm start"
  },
  "dependencies": {
    "next": "16.1.6"
  }
}
